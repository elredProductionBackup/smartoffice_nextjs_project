/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "assets-pretest.elred.io",
      "randomuser.me",
    ],
  },
};

export default nextConfig;
